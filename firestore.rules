rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Acesso permitido apenas para usuarios autenticados (incluindo anonimos).
    // O cliente faz signInAnonymously() automaticamente antes de acessar o